apiVersion: v1
kind: Service
metadata:
  name: {{.Values.prometheus.name}}
  namespace: {{.Values.namespace.name}}
spec:
  clusterIP: None
  selector:
    app: {{.Values.prometheus.name}}
  ports:
    - name: web
      port: 9090
